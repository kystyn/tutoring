#include <iostream>
#include <queue>

using namespace std;

queue<int> mergeQueues( queue<int> &q1, queue<int> &q2 )
{
    queue<int> q;
    int n = 0, size = q1.size() + q2.size();


    while (n < size)
    {
        //if (q1.front() < q2.хоро)
    }
}

int main()
{
    return 0;
}
